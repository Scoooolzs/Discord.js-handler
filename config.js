module.exports = {
  botInfo: {
    token: "", // The Bot Token
    clientId: "", // The Bot ID
  },
  global_slash: true, // `true`: Global Commands // `false`: Guild Commands (must fill in the guild_id)
  guild_id: "", // if you use Guild Commands you must fill the Guild ID in here.
};
